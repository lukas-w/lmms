ARG TAG
FROM lmmsci/ubuntu:${TAG}

ADD tmp/ubuntu-mingw.repos /tmp/repos
RUN bash /tmp/repos
ADD tmp/ubuntu-mingw.packages /tmp/packages
RUN apt-get update -yy && apt-get install -y $(cat /tmp/packages) && rm -rf /var/lib/apt/lists/*
